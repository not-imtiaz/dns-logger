#Invisible Packet Logger

A simple network tool to capture DNS queries on your home netword and save them to a local SQLite database.

## Prerequisites
- Python 3.x
- [Scapy](https://scapy.net/)
- Administrative/Root privileges (required for network sniffing)

## Installation
1. Clone the repository.
2. Install dependencies:
'''bash
pip install -r requirements.txt